# prek configuration for Rust projects
# https://github.com/j178/prek

minimum_prek_version: 0.2.0
default_install_hook_types: [pre-commit, pre-push]

repos:
  - repo: "https://github.com/pre-commit/pre-commit-hooks"
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
        exclude: "^web/dist/.*"
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-added-large-files
        args: ["--maxkb=500"]
        exclude: "^docs/.*demo.*"
      - id: check-merge-conflict
      - id: mixed-line-ending
        args: ["--fix=lf"]

  - repo: https://github.com/YuniqueUnic/autocorrect-pre-commit
    rev: 2.14.0
    hooks:
      - id: autocorrect-fix

  - repo: https://github.com/bneijt/deno-pre-commit
    rev: 2.6.1
    hooks:
      - id: deno-fmt
        exclude_types: ["javascript", "jsx", "ts", "tsx", "json"]
        exclude: "(^|/)CHANGELOG\\.md$"

  - repo: local
    hooks:
      - id: ui-colors
        name: Slint semantic color gate
        entry: cargo run -p xtask -- ui-colors
        language: system
        files: '\.slint$'
        pass_filenames: false

  - repo: local
    hooks:
      - id: rustfmt
        name: rustfmt
        entry: cargo fmt --all --check
        language: system
        types: [rust]
        pass_filenames: false

  - repo: local
    hooks:
      - id: clippy
        name: clippy
        entry: cargo clippy --workspace --all-targets -- -D warnings
        language: system
        types: [rust]
        pass_filenames: false

  - repo: local
    hooks:
      - id: rustfmt-pre-push
        name: rustfmt (pre-push)
        entry: cargo fmt --all --check
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]

  - repo: local
    hooks:
      - id: cargo-check
        name: cargo check
        entry: cargo check --workspace --all-targets
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]

  - repo: local
    hooks:
      - id: cargo-test
        name: cargo nextest
        entry: cargo nextest run --workspace --all-targets
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: cargo-doc-test
        name: cargo doc tests
        entry: cargo test --workspace --doc
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: clippy-pre-push
        name: clippy (pre-push)
        entry: cargo clippy --workspace --all-targets -- -D warnings
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: cargo-deny
        name: cargo deny
        entry: cargo deny check
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: cargo-build-release
        name: cargo build (release)
        entry: cargo build --workspace --all-targets --release
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: xtask-size
        name: xtask size gate
        entry: cargo run -p xtask -- size
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]

  - repo: https://github.com/crate-ci/typos
    rev: v1.41.0
    hooks:
      - id: typos

  - repo: local
    hooks:
      - id: forbid-env-files
        name: Block .env files
        entry: "Blocking .env files from being committed"
        language: fail
        files: '^\.env(\.|$)'
        exclude: '^\.env\.docker$'
