## Scripts

Saymore keeps required maintenance tasks in Rust under `crates/xtask`.

Use:

```bash
cargo run -p xtask -- size
```

Shell scripts under this directory are legacy helpers and should not be part of
the required workflow.
